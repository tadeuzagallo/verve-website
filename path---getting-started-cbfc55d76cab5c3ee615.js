webpackJsonp([0x6993ce0c64e3],{"./node_modules/json-loader/index.js!./.cache/json/getting-started.json":function(e,n){e.exports={data:{markdownRemark:{html:'<h1>Getting Started with Verve</h1>\n<p>For now, the only way to install Verve is to build it from source.</p>\n<h2>Installing from Source</h2>\n<p>First, you\'ll need to clone the project:</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>$ <span class="token function">git</span> clone https://github.com/tadeuzagallo/verve-lang\n</code></pre>\n      </div>\n<p>Verve is written in <a href="https://www.haskell.org/">Haskell</a> and built using the Haskell Tool <a href="https://docs.haskellstack.org/">Stack</a>, so you\'ll need to install it in order to build the project. You can refer to the <a href="https://docs.haskellstack.org/">Stack documentation</a>, but the TL;DR is that you can install it using <a href="https://brew.sh/">brew</a> if you use macOS:</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>$ brew <span class="token function">install</span> stack\n</code></pre>\n      </div>\n<p>Before building the project for the first time, you\'ll need to use <code>stack</code> to setup your environment:</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>$ stack setup\n</code></pre>\n      </div>\n<p>Once that\'s done, you should be all set to actually build verve:</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>$ stack build\n</code></pre>\n      </div>\n<p>You can try running all the tests to check that your build works (it should only take a few seconds):</p>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>$ <span class="token function">make</span> <span class="token function">test</span>\n</code></pre>\n      </div>\n<p>If everything is working fine, you should now be able to run your verve programs. There a few ways one can use it:</p>\n<ul>\n<li>Install it and just call <code>verve</code>:</li>\n</ul>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code><span class="token comment" spellcheck="true"># from within the verve repo</span>\n$ <span class="token function">ln</span> -s <span class="token variable"><span class="token variable">$(</span><span class="token function">pwd</span><span class="token variable">)</span></span>/verve /usr/local/bin\n\n<span class="token comment" spellcheck="true"># then you can run a file</span>\n$ verve file.vrv\n\n<span class="token comment" spellcheck="true"># or start the REPL:</span>\n$ verve\n</code></pre>\n      </div>\n<ul>\n<li>Directly using the wrapper script that ships with Verve:</li>\n</ul>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>$ path/to/verve file.vrv\n</code></pre>\n      </div>\n<ul>\n<li>Directly using <code>stack</code></li>\n</ul>\n<div class="gatsby-highlight">\n      <pre class="language-bash"><code>$ stack <span class="token function">exec</span> verve -- file.vrv\n</code></pre>\n      </div>\n<p>(If you had any issues following these steps, it would be great if you could <a href="https://github.com/tadeuzagallo/verve-lang/issues">create an issue</a>. Thanks!)</p>\n<h2>Contributing</h2>\n<p>The language is still constantly changing, but if you too would like hack on it, feel free to open an <a href="">issue</a> or send a <a href="https://github.com/tadeuzagallo/verve-lang/pulls">pull request</a> with your patch. Any contribution is welcome.</p>\n<p>If you decide to contribute any code, please make sure that your change include at least one test that would not pass without your patch. Tests are written using LLVM <a href="https://llvm.org/docs/CommandGuide/lit.html">lit</a> and you can find a number of existing tests in the <a href="https://github.com/tadeuzagallo/verve-lang/tree/master/tests">repo</a>.</p>'}},pathContext:{path:"/getting-started"}}}});
//# sourceMappingURL=path---getting-started-cbfc55d76cab5c3ee615.js.map